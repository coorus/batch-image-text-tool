# 变更记录

本项目采用 `主版本.次版本.修订号` 的版本格式。

## v1.3.1 · 2026-09-19

- 修复配置预设弹窗内容超出窗口时无法滚动，导致工作文件夹预设操作区被遮挡的问题。

## v1.3.0 · 2026-09-19

- 新增随工作文件夹分享的预设：`presets/manifest.json` 存参数，`presets/assets/` 存原始底图、字体与缩略图。
- 支持连接工作文件夹后保存当前配置、同步已有本机预设；后续保存或覆盖本机预设时会同步写入连接的文件夹。
- 接收者选择工作文件夹后可载入其中的预设；支持只读打开文件夹，以便在不支持写入 API 的浏览器中使用。
- 工作文件夹预设支持载入、覆盖、设为文件夹默认及删除。

## v1.2.0 · 2026-09-19

- 支持单个预设导出为 JSON 文件，其他人在工具中导入后即可保存为本机默认预设。
- 支持把预设、原始底图、本机字体与工具打包成独立 HTML 网页；接收者双击打开即自动载入，无需另带文件夹。
- 共享网页中的预设可再次载入、保存到本机或导出；导出前明确提示文件中包含的私人内容。

## v1.1.2 · 2026-09-19

- 保存预设前显示当前配置摘要，包括底图、批量文字、文字样式、区域、下边线及输出设置。
- 覆盖预设改为两步确认，明确显示要覆盖的目标，并支持在确认前修改名称。

## v1.1.1 · 2026-09-19

- 为带底图的配置预设增加缩略图预览。
- 旧版已保存预设会在首次打开列表时补生成轻量缩略图。

## v1.1.0 · 2026-09-19

- 增加本地配置预设，最多保存 10 组。
- 最近保存的预设自动成为下次启动时的默认配置；也可手动切换默认项。
- 保存字体、文字区域、批量内容及输出参数；保存时有底图或本机字体文件，会随预设一并记住。
- 支持载入、覆盖、删除预设；空间不足或浏览器禁止本地存储时会提示失败。

## v1.0.0 · 2026-09-18

首个正式发布版本。

### 功能

- 支持在原始分辨率底图上批量写入文字。
- 支持字体文件、文字样式、对齐方式和文字下边线设置。
- 支持文字区域拖动、八向缩放、滚轮缩放和方向键微调。
- 支持画布缩放、平移及两种滚轮缩放模式。
- 支持当前图片复制、单张下载和文件夹批量导出。
- 支持 PNG、JPEG、WebP 及分段图片质量设置。

### 发布整理

- 统一发布文件命名与目录结构。
- 增加本地启动入口、应用图标、版本文件与完整使用说明。
- 将静态资源缓存标识统一为 `v1.0.0`。
